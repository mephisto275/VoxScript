set names equal to new list.

push "Peter" onto names.
push "Joseph" onto names.
push "Jared" onto names named "Gohr".

loop i from 0 to length of names:
	print names at i.

loop over names into name named i:
	print "" append i append " " append name.
